"""Fixture assets for the test suite (SPEC §8). Not itself a test; imported by tests that need
one of the fixtures below as a real Python object rather than a file on disk — see
`tests/fixtures/llm/echo_backend.py`.
"""

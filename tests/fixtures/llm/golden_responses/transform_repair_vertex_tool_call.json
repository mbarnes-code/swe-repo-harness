{
  "_provenance": "Hand-constructed to match Claude-on-Vertex-AI's `:rawPredict` response, documented at https://docs.anthropic.com/en/api/claude-on-vertex-ai as the Anthropic Messages API body (Vertex adds only an `anthropic_version` discriminator to the REQUEST; the response is the native Messages shape), fed as-is (minus this key) to `vertex.parse_reply()` in src/fleet/llm/backends/vertex.py, whose `_content_blocks`/`_usage`/`_finish_reason` read exactly these snake_case top-level keys (`content`, `stop_reason`, `usage.input_tokens`/`usage.output_tokens`) — the same shape that module's own `raw_predict_body()` test helper builds. Rung: TOOL_CALL, matching `build_body()`'s tool branch. Envelope shape is identical to the REPO_CLASSIFY fixture in this same directory (`vertex_tool_call.json`) by design: `_TOOL_NAME = \"emit_response\"` (src/fleet/llm/backends/vertex.py:92) is a role-agnostic constant and `parse_reply` never introspects `content[0].input`'s internal shape, so only the `input` payload below (an `LlmPatchProposal`-schema object) differs. `files` carries TWO entries (a build-file fix and its matching version-catalog fix) to exercise the same multi-entry nested-object-tuple shape as the other two TOOL_CALL fixtures, with realistic unified diffs.",
  "id": "msg_vrtx_01XformRepairMn23Op45",
  "type": "message",
  "role": "assistant",
  "model": "claude-sonnet-5@20250929",
  "content": [
    {
      "type": "tool_use",
      "id": "toolu_vrtx_01XformRepairMn23Op",
      "name": "emit_response",
      "input": {
        "files": [
          {
            "path": "widget-core/build.gradle",
            "diff": "--- a/widget-core/build.gradle\n+++ b/widget-core/build.gradle\n@@ -12,7 +12,7 @@\n dependencies {\n-    implementation 'com.acme.widget:widget-core-legacy:2.9.0'\n+    implementation 'com.acme.widget:widget-core:3.4.1'\n }\n"
          },
          {
            "path": "gradle/libs.versions.toml",
            "diff": "--- a/gradle/libs.versions.toml\n+++ b/gradle/libs.versions.toml\n@@ -4,7 +4,7 @@\n [versions]\n-widget-core = \"2.9.0\"\n+widget-core = \"3.4.1\"\n"
          }
        ],
        "approach_summary": "Repoint widget-core/build.gradle and the version catalog at the current widget-core coordinate and version",
        "rationale": "build.gradle still referenced the retired widget-core-legacy artifact at 2.9.0, which the version catalog no longer resolves; both files must move together or the catalog and the build file disagree on which version is canonical"
      }
    }
  ],
  "stop_reason": "tool_use",
  "stop_sequence": null,
  "usage": {
    "input_tokens": 372,
    "output_tokens": 90
  }
}

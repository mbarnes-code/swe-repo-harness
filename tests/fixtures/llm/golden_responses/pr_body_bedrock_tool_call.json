{
  "_provenance": "Hand-constructed to match the AWS Bedrock Runtime `Converse` API response shape documented at https://docs.aws.amazon.com/bedrock/latest/APIReference/API_runtime_Converse.html (`output.message.content[].toolUse`, `stopReason`, `usage.{inputTokens,outputTokens,totalTokens}`), fed as-is (minus this key) to `bedrock.parse_reply()` in src/fleet/llm/backends/bedrock.py — the same Mapping shape that module's own `converse_body()` test helper builds. Rung: TOOL_CALL, matching `build_request()`'s `toolConfig` branch. Envelope shape is identical to the REPO_CLASSIFY fixture in this same directory (`bedrock_tool_call.json`) by design: `_TOOL_NAME = \"emit_response\"` (src/fleet/llm/backends/bedrock.py:71) is a role-agnostic constant and `parse_reply` never introspects `toolUse.input`'s internal shape, so only the `input` payload below (a `PrBody`-schema object) differs. This fixture is also the Rule-12 mutation base for `test_a_pr_body_reply_missing_its_required_body_field_fails_schema_validation` in `tests/test_llm_golden_responses.py` (deletes `body`, the one field on `PrBody` with no default).",
  "output": {
    "message": {
      "role": "assistant",
      "content": [
        {
          "toolUse": {
            "toolUseId": "tooluse_PrBody01ZjIwYjk4NTA",
            "name": "emit_response",
            "input": {
              "body": "Vendors the left-pad npm package into the monorepo as a leaf dependency. No downstream consumers exist yet.",
              "highlights": ["no downstream consumers", "leaf dependency"]
            }
          }
        }
      ]
    }
  },
  "stopReason": "tool_use",
  "usage": {
    "inputTokens": 320,
    "outputTokens": 30,
    "totalTokens": 350
  },
  "metrics": {
    "latencyMs": 540
  }
}

{
  "_provenance": "Hand-constructed to match Claude-on-Vertex-AI's `:rawPredict` response, documented at https://docs.anthropic.com/en/api/claude-on-vertex-ai as the Anthropic Messages API body (Vertex adds only an `anthropic_version` discriminator to the REQUEST; the response is the native Messages shape), fed as-is (minus this key) to `vertex.parse_reply()` in src/fleet/llm/backends/vertex.py, whose `_content_blocks`/`_usage`/`_finish_reason` read exactly these snake_case top-level keys (`content`, `stop_reason`, `usage.input_tokens`/`usage.output_tokens`) — the same shape that module's own `raw_predict_body()` test helper builds. Rung: TOOL_CALL, matching `build_body()`'s tool branch. Envelope shape is identical to the REPO_CLASSIFY fixture in this same directory (`vertex_tool_call.json`) by design: `_TOOL_NAME = \"emit_response\"` (src/fleet/llm/backends/vertex.py:92) is a role-agnostic constant and `parse_reply` never introspects `content[0].input`'s internal shape, so only the `input` payload below (a `VersionConflictResolution`-schema object) differs. `mechanism` is the field with `pattern=r\"^(bazel_dep|single_version_override)$\"` on `src/fleet/llm/schemas.py:281-285`; this fixture's value, `\"bazel_dep\"`, genuinely matches that pattern.",
  "id": "msg_vrtx_01ConflictResMn23Op45",
  "type": "message",
  "role": "assistant",
  "model": "claude-sonnet-5@20250929",
  "content": [
    {
      "type": "tool_use",
      "id": "toolu_vrtx_01ConflictResMn23Op",
      "name": "emit_response",
      "input": {
        "coord_key": "gradle:com.widget:widget-core",
        "proposed_version": "3.4.1",
        "mechanism": "bazel_dep",
        "violated_specs": ["[3.0,3.4)"],
        "rationale": "One consumer pins widget-core within [3.0,3.4); no version satisfies both that pin and the newer feature requiring >=3.4.1, so 3.4.1 is proposed and the older pin is disclosed as violated."
      }
    }
  ],
  "stop_reason": "tool_use",
  "stop_sequence": null,
  "usage": {
    "input_tokens": 388,
    "output_tokens": 44
  }
}

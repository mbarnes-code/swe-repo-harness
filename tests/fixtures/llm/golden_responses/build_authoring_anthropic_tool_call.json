{
  "_provenance": "Hand-constructed to match the Anthropic Messages API wire response documented at https://docs.anthropic.com/en/api/messages, and validated at test time through the real `anthropic` SDK's own `anthropic.types.Message.model_validate()` (installed SDK version 0.121.0) before being handed to `AnthropicBackend`'s real `_reply_from`. Rung: TOOL_CALL — a mandatory `emit_response` tool call, matching `_rung()`'s TOOL_CALL branch in src/fleet/llm/backends/anthropic.py. Envelope shape is identical to the REPO_CLASSIFY fixture in this same directory (`anthropic_tool_call.json`) by design: `_TOOL_NAME = \"emit_response\"` (src/fleet/llm/backends/anthropic.py:110) is a role-agnostic constant, and `_reply_from` never introspects `input`'s internal shape, so only the `input` payload below (a `BuildFileProposal`-schema object) differs. `BUILD_AUTHORING` is the §3.3 escape hatch fired when no `EcosystemAdapter` can derive targets (src/fleet/llm/schemas.py:251); this fixture's scenario is `java/acme/legacy-shim`, a pre-Maven vendored Java source drop with no pom.xml or other recognized manifest. `targets` is `BuildFileProposal`'s TWO-LEVEL nested-object tuple (each entry a `BuildTargetProposal`, src/fleet/llm/schemas.py:239-247) — this fixture deliberately carries TWO target entries (a `java_library` and its matching `java_test`), and the first entry's `srcs`/`deps`/`visibility` are all non-empty, so the round-trip genuinely exercises every one of `BuildTargetProposal`'s three sibling tuple fields plus the outer `targets` tuple, not just the outer nesting level.",
  "id": "msg_01BuildAuthoringAaBbCc",
  "type": "message",
  "role": "assistant",
  "model": "claude-sonnet-5-20250929",
  "content": [
    {
      "type": "tool_use",
      "id": "toolu_01BuildAuthoringAaBbCc",
      "name": "emit_response",
      "input": {
        "package_path": "java/acme/legacy-shim",
        "targets": [
          {
            "name": "legacy-shim",
            "rule": "java_library",
            "srcs": ["StringHelper.java", "LegacyUtil.java"],
            "deps": ["//java/acme/common:acme-base"],
            "visibility": ["//java/acme:__subpackages__"]
          },
          {
            "name": "legacy-shim_test",
            "rule": "java_test",
            "srcs": ["LegacyShimTest.java"],
            "deps": [":legacy-shim", "//third_party/junit"],
            "visibility": ["//visibility:private"]
          }
        ],
        "rationale": "The directory has no pom.xml or other ecosystem manifest -- it is a pre-Maven vendored source drop -- so no EcosystemAdapter can derive targets; a java_library plus its matching java_test is authored directly from the source layout."
      }
    }
  ],
  "stop_reason": "tool_use",
  "stop_sequence": null,
  "usage": {
    "input_tokens": 430,
    "output_tokens": 118,
    "cache_creation_input_tokens": 0,
    "cache_read_input_tokens": 0
  }
}

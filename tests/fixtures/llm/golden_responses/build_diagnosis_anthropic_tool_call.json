{
  "_provenance": "Hand-constructed to match the Anthropic Messages API wire response documented at https://docs.anthropic.com/en/api/messages, and validated at test time through the real `anthropic` SDK's own `anthropic.types.Message.model_validate()` (installed SDK version 0.121.0) before being handed to `AnthropicBackend`'s real `_reply_from`. Rung: TOOL_CALL — a mandatory `emit_response` tool call, matching `_rung()`'s TOOL_CALL branch in src/fleet/llm/backends/anthropic.py. Envelope shape is identical to the REPO_CLASSIFY fixture in this same directory (`anthropic_tool_call.json`) by design: `_TOOL_NAME = \"emit_response\"` (src/fleet/llm/backends/anthropic.py:110) is a role-agnostic constant, and `_reply_from` never introspects `input`'s internal shape, so only the `input` payload below (an `LlmBuildDiagnosis`-schema object, including its enum-valued `failure_class` field and its tuple-valued `suspect_paths` field, both structurally the same shape as fields already proven in the REPO_CLASSIFY/PR_BODY fixtures) differs.",
  "id": "msg_01BuildDiagAAABBBCCCDDDEEEF",
  "type": "message",
  "role": "assistant",
  "model": "claude-sonnet-5-20250929",
  "content": [
    {
      "type": "tool_use",
      "id": "toolu_01BuildDiagAAABBBCCCDDDEEE",
      "name": "emit_response",
      "input": {
        "failure_class": "BUILD_ERROR",
        "root_cause": "pom.xml is missing a dependency declaration for commons-lang3, which src/main/java/com/acme/Util.java imports",
        "suspect_paths": ["pom.xml", "src/main/java/com/acme/Util.java"],
        "suggested_action": "add org.apache.commons:commons-lang3 to pom.xml's <dependencies>",
        "confidence": 0.82
      }
    }
  ],
  "stop_reason": "tool_use",
  "stop_sequence": null,
  "usage": {
    "input_tokens": 410,
    "output_tokens": 46,
    "cache_creation_input_tokens": 0,
    "cache_read_input_tokens": 0
  }
}

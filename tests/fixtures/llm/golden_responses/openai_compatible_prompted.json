{
  "_provenance": "Hand-constructed to match the OpenAI-compatible chat-completions response shape (https://platform.openai.com/docs/api-reference/chat/object) that vLLM/llama.cpp/TGI/Ollama and the hosted OpenAI API all serve, fed as-is (minus this key) to `openai_compatible.parse_reply()` in src/fleet/llm/backends/openai_compatible.py, whose `_choice`/`_first_message`/`_finish_reason` read exactly `choices[0].message.content` and `choices[0].finish_reason` — the same shape that module's own `body()` test helper builds. Rung: PROMPTED (the REQUIRED rung per SPEC's ADR-0013 contract layer) — this backend's declared_capabilities().structured_output_modes is (PROMPTED,) ONLY (no supports_tools), so `message.content` carries the schema answer as bare JSON text rather than a tool call, matching `_prepare_messages()`'s PROMPTED branch in src/fleet/llm/client.py and validated the same way `_validate()` validates any PROMPTED reply: `json.loads(text)` then `response_model.model_validate(...)`.",
  "id": "chatcmpl-8f3b2c1a9d7e4f6b",
  "object": "chat.completion",
  "created": 1735689600,
  "model": "local-model",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "{\"ecosystem\": \"pypi\", \"is_library\": false, \"confidence\": 0.75, \"rationale\": \"setup.py declares no packages= and only ships a __main__ entrypoint; nothing else imports this coordinate\"}"
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 420,
    "completion_tokens": 47,
    "total_tokens": 467
  }
}

{
  "_provenance": "Hand-constructed to match Claude-on-Vertex-AI's `:rawPredict` response, documented at https://docs.anthropic.com/en/api/claude-on-vertex-ai as the Anthropic Messages API body (Vertex adds only an `anthropic_version` discriminator to the REQUEST; the response is the native Messages shape), fed as-is (minus this key) to `vertex.parse_reply()` in src/fleet/llm/backends/vertex.py, whose `_content_blocks`/`_usage`/`_finish_reason` read exactly these snake_case top-level keys (`content`, `stop_reason`, `usage.input_tokens`/`usage.output_tokens`) -- the same shape that module's own `raw_predict_body()` test helper builds. Rung: TOOL_CALL, matching `build_body()`'s tool branch. Envelope shape is identical to the REPO_CLASSIFY fixture in this same directory (`vertex_tool_call.json`) by design: `_TOOL_NAME = \"emit_response\"` (src/fleet/llm/backends/vertex.py:92) is a role-agnostic constant and `parse_reply` never introspects `content[0].input`'s internal shape, so only the `input` payload below (a `BuildFileProposal`-schema object) differs. This fixture's scenario is `gradle/tools/codegen-legacy`, a code-generation tool that predates the module's Gradle adoption and carries no build.gradle of its own -- one target whose `srcs`, `deps`, and `visibility` are all non-empty, exercising every `BuildTargetProposal` tuple field a second, independent way from the anthropic fixture's multi-target case.",
  "id": "msg_vrtx_01BuildAuthoringMn23Op",
  "type": "message",
  "role": "assistant",
  "model": "claude-sonnet-5@20250929",
  "content": [
    {
      "type": "tool_use",
      "id": "toolu_vrtx_01BuildAuthoringMn23",
      "name": "emit_response",
      "input": {
        "package_path": "gradle/tools/codegen-legacy",
        "targets": [
          {
            "name": "codegen-legacy",
            "rule": "java_library",
            "srcs": ["Codegen.java", "Templates.java"],
            "deps": ["//gradle/tools/common:codegen-support"],
            "visibility": ["//visibility:public"]
          }
        ],
        "rationale": "codegen-legacy predates the module's Gradle adoption and carries no build.gradle of its own -- no EcosystemAdapter can derive it -- so its java_library target is authored directly from its source layout, folding in the shared codegen-support dependency other legacy tools already declare."
      }
    }
  ],
  "stop_reason": "tool_use",
  "stop_sequence": null,
  "usage": {
    "input_tokens": 388,
    "output_tokens": 84
  }
}

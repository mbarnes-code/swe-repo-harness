{
  "_provenance": "Hand-constructed to match the OpenAI-compatible chat-completions response shape (https://platform.openai.com/docs/api-reference/chat/object) that vLLM/llama.cpp/TGI/Ollama and the hosted OpenAI API all serve, fed as-is (minus this key) to `openai_compatible.parse_reply()` in src/fleet/llm/backends/openai_compatible.py, whose `_choice`/`_first_message`/`_finish_reason` read exactly `choices[0].message.content` and `choices[0].finish_reason` — the same shape that module's own `body()` test helper builds. Rung: PROMPTED (the REQUIRED rung per SPEC's ADR-0013 contract layer) — this backend's declared_capabilities().structured_output_modes is (PROMPTED,) ONLY, so `message.content` carries the schema answer as bare JSON text rather than a tool call. Envelope shape is identical to the REPO_CLASSIFY fixture in this same directory (`openai_compatible_prompted.json`) by design: `parse_reply` never introspects the JSON text's internal shape beyond `json.loads`, so only the encoded payload below (a `ManifestExtraction`-schema object) differs. This fixture deliberately exercises `unparsed_reason`'s decline branch (src/fleet/llm/schemas.py:148, `default=None`) with an EMPTY `dependencies` tuple: setup.py computes its version via a `subprocess` call this role cannot execute, so the model declines rather than inventing a plausible coordinate list — the schema's own docstring names an invented extraction 'worse than no extraction' because it produces graph edges nothing can refute.",
  "id": "chatcmpl-ManifestExt8f3b2c1a9d7e",
  "object": "chat.completion",
  "created": 1735690150,
  "model": "local-model",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "{\"ecosystem\": \"pypi\", \"dependencies\": [], \"unparsed_reason\": \"setup.py computes the version and install_requires list via a subprocess call to a build-time script; static extraction cannot evaluate it and a guessed list would be unrefutable\"}"
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 210,
    "completion_tokens": 36,
    "total_tokens": 246
  }
}
